// HONEYMOON — Checkout (subscription alias)
// Re-exports the existing checkout page so it can also be reached at
// /subscription/checkout without duplicating logic.

export { default } from "@/pages/checkout/Checkout";
